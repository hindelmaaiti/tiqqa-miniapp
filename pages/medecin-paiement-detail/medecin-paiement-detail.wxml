<view class="page">
  <view class="mobile-card">
    <view class="content-scroll">
      <view class="status-row">
        <device-status-bar fillStatusBar="{{true}}" edgePadding="{{false}}" />
      </view>
      <view class="top-bar top-bar--detail">
        <view class="back-wrap" bindtap="onBack">
          <view class="back-circle">←</view>
          <text class="back-text">Retour</text>
        </view>
        <image class="logo" src="/assets/logo/logo-tiqqa.png" mode="aspectFit"></image>
        <view class="avatar-bubble">
          <view class="avatar-wrap avatar-wrap--bubble">
            <image class="avatar" src="/assets/avatar/avatar-user.png" mode="aspectFill"></image>
          </view>
          <view class="avatar-bubble-tail"></view>
        </view>
      </view>

      <view class="payment-hero-frame">
        <view class="hero-strip hero-strip--top"></view>
        <view class="payment-hero">
          <image class="hero-img" src="/assets/hero/hero-home2.png" mode="scaleToFill"></image>
          <view class="hero-overlay">
            <text class="hero-title">PRÉSERVEZ VOTRE CAPITAL SANTÉ</text>
            <view class="hero-cta" bindtap="onHeroCta">
              <text class="hero-cta-text">ACCÉDER</text>
            </view>
          </view>
        </view>
        <view class="hero-strip hero-strip--bottom"></view>
      </view>

      <view class="mint-from-hero">
        <view class="detail-mint">
          <view class="payment-detail-card">
            <view class="detail-nav-arrow detail-nav-arrow--corner detail-nav-arrow--left" bindtap="onCardNavPrev" hover-class="detail-nav-arrow--active" hover-stay-time="80">←</view>
            <view class="detail-nav-arrow detail-nav-arrow--corner detail-nav-arrow--right" bindtap="onCardNavNext" hover-class="detail-nav-arrow--active" hover-stay-time="80">→</view>
            <view class="detail-card-header">
              <text class="detail-card-title">Mode de paiement</text>
            </view>
            <view class="detail-divider"></view>

            <view class="method-pill">
              <image class="method-pill-icon" src="{{iconSrc}}" mode="aspectFit"></image>
              <text class="method-pill-label">{{label}}</text>
            </view>

            <text class="detail-section-title">Détail de paiement</text>
            <view class="detail-rows">
              <view class="detail-row" wx:for="{{details}}" wx:key="label">
                <text class="detail-row-k">{{item.label}} :</text>
                <text class="detail-row-v">{{item.value}}</text>
              </view>
            </view>

            <view class="cgu-row" bindtap="onToggleCgu">
              <view class="cgu-box {{cguAccepted ? 'cgu-box--on' : ''}}">
                <text wx:if="{{cguAccepted}}" class="cgu-check">✓</text>
              </view>
              <text class="cgu-text">Confirmer l'acceptation de CGU</text>
            </view>
          </view>

          <view class="detail-confirm-wrap">
            <view class="detail-confirm-btn" bindtap="onConfirm">CONFIRMEZ</view>
          </view>
          <view class="detail-mint-spacer"></view>
        </view>
      </view>
    </view>

    <bottom-nav-bar variant="in-card" message-count="6" bind:navtap="onNavTap" />
  </view>

  <view class="agency-modal-root" wx:if="{{showAgencyModal}}">
    <view class="agency-modal-mask" catchtouchmove="noop"></view>
    <view class="agency-modal-panel" catchtouchmove="noop">
      <view class="agency-modal-text-wrap">
        <text class="agency-modal-para">Rendez-vous dans l'agence Tasshilat la plus proche avec votre code de paiement pour finaliser la réservation. Veuillez vous y présenter muni(e) de ce code <text class="agency-modal-code">{{paymentCode}}</text></text>
      </view>
      <view class="agency-modal-close" bindtap="onCloseAgencyModal" hover-class="agency-modal-close--active" hover-stay-time="80">
        <text class="agency-modal-x">×</text>
      </view>
    </view>
  </view>
</view>
